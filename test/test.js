'use strict';

var module = require('../src/hbs-html-tag-helpers');
var template = require('createTagBlock.hbs');
var assert = chai.assert;

describe('Your Module', function () {

    it('is a module', function () {
        assert.ok(true);
    });
});
